# Bot 設定
AGENT_NAME=gpt                  # エージェント名
COPILOT_MODEL=gpt-5.4           # 使用する AI モデル (gpt-5.4(1x), gpt-5.3-codex(1x), gpt-5.4-mini(0.33x), gpt-5-mini(FREE) など)
REASONING_EFFORT=               # 推論レベル (low/medium/high/xhigh, 空=モデルのデフォルト)

# タイムアウト設定
WAIT_TIMEOUT_MS=600000          # メッセージ待機タイムアウト (default: 10min)
SESSION_TIMEOUT_MS=3600000      # セッションタイムアウト (default: 1hour)

# ディレクトリ設定
WORKSPACE_DIR=./gpt             # AI の作業ディレクトリ
TEMPORARY_DIR=./tmp             # 添付画像等の一時保存先

# 権限設定
# 自動承認する操作 (カンマ区切り: shell,write,read,url,mcp,custom-tool / *=全て自動承認)
PERMISSION_AUTO_APPROVE=*
# ユーザー応答タイムアウト (default: 120s)
USER_RESPONSE_TIMEOUT_MS=120000

# Discord
# トークンは https://discord.com/developers/applications で取得
DISCORD_BOT_TOKEN=your_bot_token_here
